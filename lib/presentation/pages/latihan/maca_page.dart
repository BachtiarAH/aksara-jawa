import 'package:flutter/material.dart';

class LatihanMacaPage extends StatelessWidget {
  const LatihanMacaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("latihan maca page"),);
  }
}